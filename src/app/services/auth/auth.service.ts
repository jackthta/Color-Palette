import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class AuthService {
  // Determines if user is authenticated and holds authenticating functions (sign in, sign out)

  constructor() { }
}
